b' <!DOCTYPE html> <html lang="en"> <head> <meta charset="utf-8"> <meta http-equiv="X-UA-Compatible" content="IE=edge"> <meta name="viewport" content="width=device-width, initial-scale=1">  <meta name="description" content="www.paste.org - allows users to paste snippets of text, usually samples of source code, for public viewing."> <meta name="author" content="An Australian"> <link rel="icon" href="/favicon.ico"> <title>Paste code - paste.org</title>  <link href="/assets/themes/bleh/css/github-highlight.css" rel="stylesheet"> <script src="/assets/themes/bleh/js/highlight.pack.js"></script> <script>hljs.initHighlightingOnLoad();</script> </head> <body> <p><pre><code class="cpp">/* Quick Quide to C  - prepared by Mrs Chu-Poon Yap Ching on 1 Aug 01\n   Multiple line comment must end with\n*/\n// Single line comment \n\n// Preprocessor Directives before the main function -----------------------\n#include &lt;stdio.h&gt;\n#include &lt;math.h&gt;    // to use sin(), cos(), sqrt(), pow(), exp() functions\n#define PI 3.1416\n\n// main function (a simple skeleton) --------------------------------------\nvoid main (void)\n{\n // Declare variables\n // Input\n // Calculation\n // Output\n} // end main\n\n// Declaration of variables -----------------------------------------------\n int age, num ;                   \n float weight, price ;            \n double impedance, frequency ;    \n\n// Use printf and scanf to prompt for input -------------------------------\n printf(&quot;Enter an integer: &quot;);    \n scanf(&quot;%d&quot;, &amp;num;);              // use address operator \'&amp;\'\n printf(&quot;Enter 2 real numbers: &quot;);\n scanf(&quot;%f %f&quot;, &amp;x, &amp;y);         // use , to separate addresses\n printf(&quot;Enter 2 double-precision numbers: &quot;);\n scanf(&quot;%lf %lf&quot;, &amp;x, &amp;y);    // double-precision is long floating point\n \n// Use printf to output messages and values ------------------------------\n printf(&quot;A simple program to find area\\n&quot;);  // use double quotations &quot; &quot;\n printf(&quot;The number of days is %d\\n&quot;, day);     // take care of , and ;\n printf(&quot;The real number is %f\\n&quot;, x );     \n printf(&quot;The weight correct to 2 decimal places is %.2f kg\\n&quot;, weight );  \n printf(&quot;The real number in double-precision is %.2lf\\n&quot;, x );\n\n// Assignment statements and arithmetic operators ------------------------\n half = 0.5;   // do not use 1/2 (this integer division yields 0 )\n oneThird = 1.0/3;   \n sum = x+y;\n product = x*y*z ;   \n xSquare = x*x ;         \n    radian = x*PI/180;\n quotient  = 9/5;     // integer division yields 1\n remainder = 9%5;     // modulus division yields 4\n realNumber = 9.0/5;  // floating point division yields correct value 1.8\n z = x*y/(x + y) ;  //(product of x and y) divided by (sum of x and y)\n w = (x+y)/(x*y) ;  //(sum of x and y) divided by (product of x and y)\n\n// math functions must have arguments enclosed in brackets ( ) ------------\n y = 0.5 * a * b * sin(x);     // x is in radian.   \n y = 0.5 * a * b * sin(x*PI/180);  // x is in degree\n sineSquare = sin(x)*sin(x);\n xCube = pow(x, 3) ;    // x is raised to the power of 3\n area = sqrt( s*(s-a)*(s-b)*(s-c) ); // use * for multiplication\n m = exp( (n-1)/n * log(p) ) ;  // take care of the brackets\n\n// if-else may use conditions like (x&gt;y) (x&lt;y) (x==y) (x&gt;=y) (x&lt;=y) (x!=y)\n\n if ( x &gt; y )    // cannot end with ;\n {\n  printf(&quot;%d is greater than %d\\n&quot;, x, y);\n } // end if\n else\n {\n  printf(&quot;%d is less than or equal to %d\\n&quot;, x, y);\n } // end else\n\n//=====End of the Quick Guide==============================================</code></pre></p> <p align="right"><span style="font-size: 60%">Add a code snippet to your website: <a href="https://www.paste.org" target="_blank">www.paste.org</a></span></p> </body> </html>'